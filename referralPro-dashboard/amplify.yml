version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --legacy-peer-deps
    build:
      commands:
        - npm run build -- --configuration uat
  artifacts:
    baseDirectory: dist/referralPro-dashboard/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
